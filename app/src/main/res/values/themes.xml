<resources>

    <!-- Base application theme -->
    <style name="theme.WhatsAppCloneApk" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme attributes -->
    </style>

    <!-- Theme without ActionBar -->
    <style name="theme.WhatsAppCloneApk.NoActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Customize other theme attributes if needed -->
    </style>

</resources>
